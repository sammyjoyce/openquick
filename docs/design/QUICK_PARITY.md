# Shopify Quick parity matrix

Status: implementation-audited parity note  
Audience: OpenQuick maintainers and orchestrator agents  
Scope: compare public Shopify Quick capabilities/design choices to OpenQuick as of this repo state.

## Sources

1. **Primary source — Shopify Engineering:** ["Quick: An internal hosting platform for the AI era"](https://shopify.engineering/quick), June 2026. Key facts used here: folder of HTML/assets to employee-only URL; launched July 2025; 50,000+ sites; >50% employee adoption; single $200/mo VM; GCS/gcsfuse/NGINX/IAP architecture; `quick deploy` as a small `gcloud rsync` wrapper; DB/files/AI/warehouse/websockets/identity API group; zero-config browser API with server-side keys; `quick init` agent skills; no owners/anyone can overwrite; no custom backends or cron jobs; rate limiting after abuse; server migrated Node to Go; "Lehrwerkstatt" discoverability.
2. **Author thread — Daniel Beauchamp (@pushmatrix):** <https://unrollnow.com/status/2064722585019969727>. Confirms zero-config data/files/AI/websockets APIs; chosen subdomain; shared browser-callable server with zero API keys; Artifact/designer tools; websocket collab/multiplayer; 140+ game-jam submissions; shared JS libraries published as sites; single VM plus single cloud database.
3. **Hacker News submission:** <https://news.ycombinator.com/item?id=48481517>. Verified through Algolia as submission-only, 4 points, zero comments; no additional capability information.
4. **Third-party analysis — Stacktree:** <https://stacktr.ee/blog/what-shopify-quick-proves>, June 2026. Vendor commentary; confirms the primary facts, identity fields (`name`, `title`, `team`, Slack handle), Dec 2025 adoption spike, and public-internet requirements Quick deliberately skips (ownership, unguessable URLs, end-to-end gates). No new Quick capabilities beyond Source 1.
5. **Searched but empty trails:** Lobste.rs; Reddit r/programming and r/webdev; conference talks/videos/podcasts about Quick specifically; OSS clones of Quick; public quick.shopify.io docs/screenshots; HN comment discussion. These found no additional Quick capability details.

## Status vocabulary

- **parity** — OpenQuick implements the user-visible capability or intentionally equivalent design constraint today.
- **partial** — OpenQuick has useful coverage but not the full Shopify Quick behavior or not for every provider.
- **gap (implemented now)** — this change closed a previously identified parity gap.
- **gap (deferred v2/v3 by design)** — the gap is known and intentionally outside v1 for safety/cost/product-scope reasons.
- **intentionally different** — OpenQuick deliberately chooses a different self-hosted mechanism while preserving the product goal.

## Capability matrix

| Shopify Quick capability / design decision | OpenQuick status | OpenQuick evidence | Rationale |
| --- | --- | --- | --- |
| Drop a folder of HTML/assets and get a private URL with no framework/pipeline/config requirement. [S1] | parity | Product stance says "drop a folder of HTML/assets" and deploy static assets with `quick deploy` (`docs/design/WORKFLOW.md:9-24`); `quick init` scaffolds static HTML and same-origin SDK (`docs/design/WORKFLOW.md:101-162`); deploy validates output and transfers files (`src/core/ops.c:930-940`, `src/core/ops.c:1026-1094`). | OpenQuick keeps the static-folder mental model and does not infer frameworks. Privacy depends on configured IAP/trust boundary. |
| GCS bucket per folder plus `gcsfuse` mount. [S1] | intentionally different | Architecture explicitly replaces bucket/gcsfuse with staging directories, immutable releases, and current symlink (`docs/design/ARCHITECTURE.md:275-317`); implementation prepares `.incoming` and `releases` under the site directory (`server/internal/deploy/deploy.go:69-95`) and atomically activates by rename/symlink (`server/internal/deploy/deploy.go:144-224`). | Self-hosted OpenQuick avoids a required cloud bucket. The product behavior is equivalent, but the storage mechanism is not. |
| Wildcard subdomains route `mysite.quick.shopify.io` to the site folder. [S1] | parity | URL generation emits `https://<site>.<public_base_domain>` (`server/internal/sites/sites.go:135-138`); host routing extracts one-label sites under `.localhost` or `public_base_domain` (`server/internal/sites/sites.go:150-168`); static handler routes host to site (`server/internal/static/static.go:93-104`). | OpenQuick routes wildcard hosts in `quickd` rather than proxy config. |
| Subdomain of the creator's choice. [S1][S2] | partial | CLI/site config model has `subdomain` and deploy plan URL resolution (`docs/design/WORKFLOW.md:217-223`, `src/core/deploy_plan.c:493-515`); deploy command accepts `--subdomain` (`src/cli/commands_deploy.c:164-180`). Server routing and deploy catalog still use the site name as the host label (`server/internal/sites/sites.go:150-168`, `server/internal/store/store.go:225-233`). | Choosing a site name gives a chosen subdomain. Alias-style `site != subdomain` is not fully wired through quickd routing/catalog yet. |
| Identity-aware proxy gates viewing so only the trusted org can see sites. [S1] | parity | Workflow default requires every request to pass through identity-aware edge (`docs/design/WORKFLOW.md:15-24`); handler authenticates viewers before site/static/API serving (`server/internal/static/static.go:39-83`, `server/internal/static/static.go:123-131`); config supports Tailscale, Cloudflare, dev, and none adapters (`server/internal/config/config.go:35-43`, `server/internal/identity/adapter.go:25-35`). | OpenQuick is self-hosted, so the operator chooses the trust boundary. `iap=none` is available for loopback/local use, not a Shopify-style employee-only environment. |
| `quick deploy` is a tiny rsync wrapper. [S1] | parity | Design says CLI wraps rsync over SSH (`docs/design/WORKFLOW.md:17-18`); implementation calls `quickd deploy prepare`, builds an `rsync -az ...` argv, and calls `quickd deploy activate` (`src/core/ops.c:969-1001`, `src/core/ops.c:1026-1123`, `src/core/ops.c:1160-1188`). | Transport is SSH+rsync instead of `gcloud rsync`, matching OpenQuick's self-hosting goal. |
| Zero-config browser API with no client-side API keys. [S1][S2] | partial | API routes are same-origin under `/_quick/*` (`server/internal/api/handler.go:56-103`); SDK uses relative `/_quick` paths and `credentials: 'same-origin'` (`sdk/js/src/index.ts:94-135`); `quick.capabilities` reports `ai:false`, `warehouse:false` (`server/internal/api/handler.go:97-103`). | Implemented APIs are zero-config. Full Shopify Quick parity is partial until AI and warehouse exist. |
| Database collections with create/update/delete/list. [S1][S2] | parity | API dispatches `/_quick/db/:collection` (`server/internal/api/handler.go:74-80`); collection CRUD publishes create/update/delete events (`server/internal/api/handler.go:179-255`); SDK exposes `quick.db.collection(...).create/get/update/list/remove` (`sdk/js/src/index.ts:370-435`). | Backing store is SQLite instead of CloudSQL, but browser capability is present. |
| Firebase-inspired live DB subscriptions with `onCreate`, `onUpdate`, `onDelete`. [S1] | parity | SDK subscription handlers define `onCreate`, `onUpdate`, `onDelete` (`sdk/js/src/index.ts:24-38`); implementation maps DB events to realtime channel listeners (`sdk/js/src/index.ts:406-427`); API publishes DB mutations to `db:<collection>` (`server/internal/api/handler.go:230-255`). | Same user-visible pattern; no Firebase Security Rules by design. |
| File uploads API. [S1][S2] | parity | API dispatches `/_quick/uploads` (`server/internal/api/handler.go:78-80`, `server/internal/api/handler.go:325-386`); upload manager stores files under `/uploads/<site>/<yyyy>/<mm>` with size limits (`server/internal/uploads/uploads.go:23-66`); SDK exposes `quick.uploads.put/get/remove` (`sdk/js/src/index.ts:438-462`). | Implemented with local disk and SQLite metadata. |
| WebSockets for collaboration/multiplayer. [S1][S2] | parity | API exposes `GET /_quick/realtime` (`server/internal/api/handler.go:103-114`); realtime hub accepts WebSockets, enforces per-site connection and message limits, and broadcasts channels (`server/internal/realtime/hub.go:41-95`); SDK exposes `quick.realtime.channel` (`sdk/js/src/index.ts:464-474`). | Supports the same collaborative/multiplayer primitive. |
| Identity API exposes corporate context such as name/title/team/Slack handle. [S1][S4] | partial | Normalized identity includes login/name/email/avatar/groups/device/capabilities (`server/internal/identity/adapter.go:11-22`); Cloudflare maps sub/email/name/groups and custom simple string claims into `capabilities` (`server/internal/identity/cloudflare.go:64-147`); Tailscale adapters expose login/name/avatar/device and optional app capabilities (`server/internal/identity/tailscale_localapi.go:112-128`, `server/internal/identity/tailscale_serve.go:24-45`). | OpenQuick exposes what the configured provider supplies. It can surface title/team/Slack-handle-like Cloudflare Access custom claims, but cannot guarantee Shopify corporate fields for every IAP. |
| Cloudflare Access custom/group claims can carry title/team/Slack-handle-like metadata. [S4] | gap (implemented now) | Cloudflare adapter now decodes raw claims and copies up to 16 unrecognized string claims into `Identity.Capabilities` (`server/internal/identity/cloudflare.go:64-147`); test covers string copy, non-string drop, known-claim filtering, and max bound (`server/internal/identity/identity_test.go:176-224`). | Closes the provider-dependent part of identity parity for orgs that configure Access custom claims. |
| AI proxy: `quick.ai.chat`, image generation, frontier models, server-side keys. [S1][S2] | gap (deferred v2/v3 by design) | Architecture defers AI to v2 because of cost, abuse, provider secrets, and policy (`docs/design/ARCHITECTURE.md:717-725`); v2 roadmap includes AI chat/image proxy with host-managed keys (`docs/design/ARCHITECTURE.md:1167-1175`); current capabilities return `ai:false` (`server/internal/api/handler.go:97-103`) and SDK throws unavailable (`sdk/js/src/index.ts:481-492`). | Explicitly out of v1 scope. |
| Data warehouse / BigQuery pulls. [S1] | gap (deferred v2/v3 by design) | Architecture defers warehouse proxy to v2/enterprise with explicit host config and query allowlists (`docs/design/ARCHITECTURE.md:737-741`); roadmap lists optional warehouse query proxy (`docs/design/ARCHITECTURE.md:1171-1176`); current capabilities return `warehouse:false` (`server/internal/api/handler.go:97-103`). | Explicitly deferred because it bridges private analytics into arbitrary static sites. |
| `quick init` is agent-ready and ships the knowledge agents need. [S1] | parity | Workflow requires generated `AGENTS.md` and `docs/openquick-api.md` (`docs/design/WORKFLOW.md:110-166`); implementation emits agent constraints and bundled API reference (`src/core/ops.c:311-332`); `quick.json` enables `/_quick/sdk.js` by default (`src/core/ops.c:335-349`). | OpenQuick uses files agents can read instead of external docs at generation time. |
| No permissions, no site owners, anyone with deploy access can overwrite any site. [S1][S4] | parity | Workflow default states authenticated viewers can view every site and deployers can overwrite every site (`docs/design/WORKFLOW.md:15-24`); default philosophy says no owners/ACLs/approval flows (`docs/design/WORKFLOW.md:788-797`); architecture v0 cut scope includes no per-site owners (`docs/design/ARCHITECTURE.md:1137-1148`). | Matches Quick's constraint, bounded by SSH deploy access rather than public internet access. |
| Team says no to custom backends and cron; fixed small capability set. [S1] | parity | CLI-generated agent constraints say do not create/require a custom web server and use `/_quick/sdk.js` for platform APIs (`src/core/ops.c:311-317`); workflow says OpenQuick provides static sites plus shared backend, no custom infrastructure (`docs/design/WORKFLOW.md:9-13`). | There is no custom-backend or cron feature in v1. |
| Rate limiting added after abuse. [S1] | parity | Architecture requires per-site/per-identity rate limits (`docs/design/ARCHITECTURE.md:688-689`, `docs/design/ARCHITECTURE.md:924-931`); realtime hub enforces connection/message limits (`server/internal/realtime/hub.go:41-95`); SQLite schema includes `rate_limits` (`server/internal/store/store.go:140-146`). | Rate limiting exists in current realtime path and schema; broader high-cost APIs remain future work with their APIs. |
| Server migrated from Node to Go for memory/parallelism. [S1] | parity | Architecture chooses Go `quickd` and explicitly references Shopify's Node→Go lesson (`docs/design/ARCHITECTURE.md:143-156`); executable is Go `cmd/quickd` (`server/cmd/quickd/main.go:1-20`, `server/cmd/quickd/main.go:315-382`). | OpenQuick starts with Go for the host daemon. |
| Single VM / cheap single-host operation. [S1][S2] | parity | Workflow default operating model is one host running `quickd` (`docs/design/WORKFLOW.md:15-18`); `quickd serve` opens one SQLite store and serves static/API through one handler (`server/cmd/quickd/main.go:365-382`); v1 design uses one SQLite DB per host (`docs/design/ARCHITECTURE.md:595-603`). | OpenQuick matches the single-host shape. Exact monthly cost is operator-dependent and not benchmarked here. |
| Single cloud database / CloudSQL backing. [S1][S2] | intentionally different | Store schema is SQLite (`server/internal/store/store.go:94-146`); architecture says one SQLite database per host (`docs/design/ARCHITECTURE.md:595-603`). | Self-hosted OpenQuick avoids a required managed CloudSQL dependency while preserving a shared DB. |
| NGINX wildcard config maps subdomains to folders. [S1] | intentionally different | Architecture chooses built-in static routing in Go rather than NGINX/Caddy-only static serving (`docs/design/ARCHITECTURE.md:197-211`); handler routes by path fallback or host (`server/internal/static/static.go:93-104`). | Keeping routing, identity, and APIs in one Go pipeline is safer for a self-hosted product. |
| Emergent ecosystem: sites import JS directly from other sites; shared libraries have landing pages. [S1][S2] | gap (implemented now) | Static CORS now reflects only sibling site origins for GET/HEAD and sets `Vary: Origin` plus credential support (`server/internal/static/static.go:293-365`, `server/internal/static/static.go:406-459`); API CORS remains same-origin only (`server/internal/api/handler.go:120-145`); tests cover sibling reflect, non-sibling no-reflect, API unchanged, Vary (`server/internal/static/static_test.go:144-177`); user docs show publishing/importing a library site (`docs/user/quick-deploy.md:109-138`). | This enables Quick-style shared JS modules without opening `/_quick/*` to cross-origin calls. |
| "Lehrwerkstatt": every visible site teaches the next person; apex host discoverability. [S1][S4] | gap (implemented now) | Directory config defaults on (`server/internal/config/config.go:20-80`); static handler serves directory at apex root or `/~/` after viewer auth (`server/internal/static/static.go:45-56`, `server/internal/static/static.go:147-180`); template lists sites with URL/release/updated/deployer via `html/template` (`server/internal/static/static.go:205-283`); tests cover rows, escaping, disabled 404, anonymous denial (`server/internal/static/static_test.go:179-235`); user docs mention directory and disable knob (`docs/user/quick-deploy.md:140-148`). | Browser discovery now complements `quick list`/TUI and is identity-gated like sites. |

## Gaps closed in this change

1. **Cross-site shared libraries:** static GET/HEAD responses now reflect only sibling site origins and keep `/_quick/*` same-origin (`server/internal/static/static.go:293-365`, `server/internal/api/handler.go:120-145`).
2. **Browser site directory:** apex root and `/~/` now render a minimal authenticated HTML directory from the SQLite catalog; it is enabled by default and disabled with `directory.enabled=false` (`server/internal/config/config.go:20-80`, `server/internal/static/static.go:45-56`, `server/internal/static/static.go:205-283`).
3. **Cloudflare identity richness:** unrecognized simple string Access claims now flow into `Identity.Capabilities` with a 16-key cap (`server/internal/identity/cloudflare.go:64-147`).

## Known gaps kept open (with rationale)

- **AI proxy:** kept as v2 because provider secrets, abuse controls, budgets, and policy are required before exposing `quick.ai.*` (`docs/design/ARCHITECTURE.md:717-725`, `docs/design/ARCHITECTURE.md:1167-1175`).
- **Warehouse proxy:** kept as v2/enterprise because it exposes private analytics data and needs explicit allowlists (`docs/design/ARCHITECTURE.md:737-741`, `docs/design/ARCHITECTURE.md:1171-1176`).
- **True alias subdomains:** CLI accepts `subdomain`, but quickd still routes/catalogs by site name. Full `site != subdomain` serving needs catalog-aware host routing and deploy activation changes; this was outside the requested gaps.
- **Provider-specific identity richness:** OpenQuick can normalize and pass through available provider claims, but cannot synthesize Shopify-only corporate fields unless the operator's IAP supplies them.
- **Cloud-specific internals:** GCS/gcsfuse, NGINX wildcard mapping, and CloudSQL are intentionally not adopted; OpenQuick uses local releases, Go routing, and SQLite to remain self-hosted and one-host friendly.

## Matrix status counts

- parity: 14
- partial: 3
- gap (implemented now): 3
- gap (deferred v2/v3 by design): 2
- intentionally different: 3
- total rows: 25
