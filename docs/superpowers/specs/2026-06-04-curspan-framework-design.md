# Archived component-framework design note

This historical note described a pre-OpenQuick C23 CLI/TUI component-framework
experiment. It is retained only as background for the vendored `cs_` rendering
internals under `src/components/`, `src/surface/`, and `src/style/`.

Current product direction is authoritative in:

- [`docs/design/ARCHITECTURE.md`](../../design/ARCHITECTURE.md)
- [`docs/design/WORKFLOW.md`](../../design/WORKFLOW.md)

Do not treat this archived note as product guidance for OpenQuick commands,
server behavior, SDK APIs, installation, or release scope.
