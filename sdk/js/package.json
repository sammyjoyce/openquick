{
  "name": "@openquick/sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/quick.js --format esm",
    "test": "bun test test/shape.test.js",
    "typecheck": "if command -v tsc >/dev/null 2>&1; then tsc --noEmit --strict --target ES2020 --module ESNext --moduleResolution Bundler --lib ES2020,DOM src/index.ts; else echo 'typescript not found; skipping typecheck'; fi"
  }
}
